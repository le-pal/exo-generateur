# Backend
NODE_ENV=development
PORT=3001
DB_PATH=./data/exo.db
FRONTEND_URL=http://localhost:5173

# Les clés API sont gérées dans l'interface Admin (stockées en DB)
# Elles ne doivent PAS être mises ici en production
